<nav class="navbar navbar-expand-lg bg-body-tertiary mb-5">
  <div class="d-flex justify-content-between align-items-center w-100">
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag("logo.jpg", alt: "Logo", height: "300", class: 'p-0') %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link mx-2" %>
        </li>
        <% if current_user %>
        <li class="nav-item">
          <%= link_to "My Messages", about_path, class: "nav-link mx-2" %>
        </li>
        <% end %>

        <!-- Ciphers dropdown -->
        <% if current_user %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Ciphers
            </a>
            <ul class="dropdown-menu">
              <!-- Add links for each cipher -->
              <li class="nav-item"><%= link_to 'Bacon Cipher', bacon_index_path, class: 'dropdown-item' %></li>
              <li class="nav-item"><%= link_to 'Caesar Cipher', caesar_index_path, class: 'dropdown-item' %></li>
              <li class="nav-item"><%= link_to 'Vigenere Cipher', vignere_index_path, class: 'dropdown-item' %></li>
              <li class="nav-item"><%= link_to 'XOR Cipher', xor_index_path, class: 'dropdown-item' %></li>
            </ul>
          </li>
        <% end %>

                <!-- User authentication -->
        <li class="nav-item">
          <% if current_user %>
            <%= link_to 'Logout', destroy_user_session_path, class: "nav-link mx-2", data: { turbo_method: :delete } %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: 'nav-link mx-2' %>
          <% end %>
        </li>

        
      </ul>
    </div>
  </div>
</nav>
